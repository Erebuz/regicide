import {StyleSheet, Text, View} from 'react-native';

export const Court = () => {
  return (
    <View style={styles.body}>
      <Text>Faces</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  body: {
    backgroundColor: 'red',
  },
});
